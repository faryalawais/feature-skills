# Design Contract — <FEATURE-ID> <Feature title>

> Generated by the `design-contract` skill. This is a **boundary**:
> `feature-implement` may not exceed it. `governance-gate` checks against it.
> Source: <Figma frame / "PRD-derived (no Figma extract)">

## 1. Routes
- `<path>` — <purpose>

## 1a. UI registry entries
> **Every named element in §2 anatomy — interactive OR static — gets a
> `component.*` path here.** This is enforced by `npm run validate:contract`.
> Do not limit this table to buttons and inputs — named sections, carousels,
> info rows, banners, and static layout zones all need entries so that
> `bdd-scaffold` can assert their presence and `governance-gate` can verify
> they are rendered.

### Screens
| Path | Description |
|------|-------------|
| `screen.<feature>.<screenKey>` | <one-line purpose> |

### Components
| Path | Description | Parent screen | States |
|------|-------------|---------------|--------|
| `component.<feature>.<screenKey>.<componentKey>` | <one-line purpose> | `screen.<feature>.<screenKey>` | `default, disabled, …` |

> After filling this table, run `npm run validate:contract -- <id>`.
> Fix any gaps before advancing status.

## 1b. API field bindings
> Data returned from APIs or server loaders is registered in
> `tokens/api-registry.json` as `field.*` paths (see
> `tokens/templates/PRD-API-Field-Paths-and-Bindings.md`). Each field lists
> `$displaysAt` component paths. Gherkin may assert:
> `` `field.user.info.firstName` is displayed in `component.dashboard.overview.welcomeHeading` ``

### Fields introduced or consumed
| Field path | JSON Pointer | Displays at (components) | Source |
|------------|--------------|--------------------------|--------|
| `field.<domain>.<…>` | `$.…` | `component.…` | `POST /api/…` or server data |

### Explicit bindings (optional)
| Binding path | Field | Component |
|--------------|-------|-----------|
| `binding.<feature>.<…>` | `field.…` | `component.…` |

## 2. Component anatomy
> Every named element must carry a `[component.*]  data-testid` tag on its
> line. This is the machine-readable anchor that `validate:contract` parses.
> An element without this tag is invisible to the gate.
> Format:  `├─ <ElementName>  [component.X.Y.Z]  data-testid`
```
<RootComponent>  [component.<feature>.<screen>.root]  data-testid
├─ <Child>  [component.<feature>.<screen>.child]  data-testid — <role>
│  └─ <Grandchild>  [component.<feature>.<screen>.grandchild]  data-testid — <role>
└─ <Child>  [component.<feature>.<screen>.otherChild]  data-testid — <role>
```

## 3. Layout & spacing
| Container | Direction | Gap | Padding | Alignment | Sizing |
|-----------|-----------|-----|---------|-----------|--------|
| <name>    | row/column| `space.md` | `space.lg` | <align> | <fixed/fluid> |

## 4. Tokens per element
| Element | Background | Text | Border | Radius | Spacing | Font size |
|---------|-----------|------|--------|--------|---------|-----------|
| <name>  | `color.surface.base` | `color.text.default` | — | `radius.md` | `space.md` | `font.size.body` |

## 5. States
| Element | State | Visual change (in tokens) |
|---------|-------|---------------------------|
| <name>  | hover | <token change> |
| <name>  | focus | <focus ring tokens> |
| <name>  | disabled | <token change> |
| <name>  | loading / empty / error | <description> |

## 6. Responsive
| Breakpoint | What changes |
|------------|--------------|
| mobile (<640px) | <change> |
| desktop (≥640px) | <change> |

## 7. Accessibility
- Roles / landmarks: <…>
- Labels: <…>
- Focus order: <…>
- Keyboard: <…>
- Contrast: <…>

## 8. Data model
| Table | Columns touched | Read / Write |
|-------|-----------------|--------------|
| <name> | <columns> | <R/W> |

## 9. Acceptance-criteria mapping
| Acceptance criterion | Covered by scenario |
|----------------------|---------------------|
| <criterion> | <Scenario name> |

## 10. Visual reference
- `features/<FEATURE-ID>/figma/reference.png` — <present / not available>

## 11. Fidelity tolerance
- Visual-diff ratio: `0.02` (2%)

## Missing tokens
> Any value with no matching design token. If this section is non-empty,
> the design system must be extended before implementation.
- <none>
